# Pizza Hut Documentation

This directory contains the documentation source for docs.page.

- `web/` documents the Next.js customer-facing application.
- `studio/` documents the Sanity content management application.

Navigation is configured in the repository-root `docs.json`.
